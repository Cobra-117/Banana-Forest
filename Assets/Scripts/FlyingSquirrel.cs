using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlyingSquirrel : MonoBehaviour
{
    public Vector3 MapEnd;
    public Vector3 TowerPosition;

    // Start is called before the first frame update
    void Start()
    {
        //GameObject DistanceMapObj = GameObject.FindGameObjectWithTag("DistanceMap");
        //DistanceMap DistanceMapScript = DistanceMapObj.ge
        //MapEnd = new Vector3();
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    //Suit la map à l'envers
}
